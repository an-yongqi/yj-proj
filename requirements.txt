# Core ML
# torch already installed in server environment
transformers==4.42.4
accelerate==0.33.0
peft==0.10.0
datasets==2.20.0
sentencepiece>=0.1.99

# Evaluation
lm-eval==0.3.0

# LoRA composition (Nevergrad)
nevergrad>=0.11.0

# FANG clustering
k-means-constrained
scikit-learn

# Numerical & utilities
numpy==1.26.4
pandas
tqdm
safetensors
einops
protobuf

# ReST-KV evaluation metrics
fuzzywuzzy
python-Levenshtein
rouge
rouge-score
jieba

# Visualization (optional)
seaborn
matplotlib

# Flash Attention (optional, requires CUDA)
# pip install flash-attn==2.5.8 --no-build-isolation
